version: "3.9"

services:
  alice-gpt:
    build: .
    container_name: alice-gpt
    restart: unless-stopped

    ports:
      - "127.0.0.1:3002:8080"

    environment:
      OPENAI_API_KEY: "${OPENAI_API_KEY}"

    logging:
      options:
        max-size: "10m"
        max-file: "3"
